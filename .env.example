NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-5
ALLOWED_EMAIL=rdarugar@usc.edu
# One-time admin bootstrap (only needed to run scripts/create-admin-user.ts; not read by the deployed app)
ADMIN_PASSWORD=
# Phase 2+
KIE_API_KEY=
CRON_SECRET=
# Phase 3
BUFFER_TOKEN_1=
BUFFER_TOKEN_2=
# Majordomo spend tracking (optional — calls go direct to Anthropic if unset).
# To enable in a deployed environment, add this var in the Vercel project settings too (not just .env.local) —
# decide production vs. preview deliberately per environment, don't set it everywhere by default.
MAJORDOMO_API_KEY=
